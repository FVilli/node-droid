name: Build Core Image

on:
  push:

jobs:
  meta:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      repo: ${{ steps.image.outputs.repo }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract core version tag
        id: meta
        run: |
          python - <<'PY'
          import json
          import os
          import re

          path = os.environ.get("GITHUB_EVENT_PATH")
          with open(path, "r", encoding="utf-8") as f:
            payload = json.load(f)

          messages = []
          head = payload.get("head_commit") or {}
          if head.get("message"):
            messages.append(head["message"])
          for c in payload.get("commits", []):
            if c.get("message"):
              messages.append(c["message"])

          tag = ""
          for msg in messages:
            m = re.search(r"\[core\]\s*v(\d+\.\d+\.\d+)", msg)
            if m:
              tag = m.group(1)
              break

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
            out.write(f"tag={tag}\n")
          PY

      - name: Compute lowercase image repo
        id: image
        run: |
          echo "repo=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

  build-core:
    runs-on: ubuntu-latest
    needs: meta
    if: needs.meta.outputs.tag != ''
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push core image
        uses: docker/build-push-action@v6
        with:
          context: core
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ needs.meta.outputs.repo }}/core:${{ needs.meta.outputs.tag }}
            ghcr.io/${{ needs.meta.outputs.repo }}/core:latest
