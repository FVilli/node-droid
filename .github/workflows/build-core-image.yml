name: Build Core Image

on:
  push:

jobs:
  build-core:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract core version tag
        id: meta
        run: |
          python - <<'PY'
          import json
          import os
          import re

          path = os.environ.get("GITHUB_EVENT_PATH")
          with open(path, "r", encoding="utf-8") as f:
            payload = json.load(f)

          messages = []
          head = payload.get("head_commit") or {}
          if head.get("message"):
            messages.append(head["message"])
          for c in payload.get("commits", []):
            if c.get("message"):
              messages.append(c["message"])

          tag = ""
          for msg in messages:
            m = re.search(r"\[core\]\s*v(\d+\.\d+\.\d+)", msg)
            if m:
              tag = m.group(1)
              break

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as out:
            out.write(f"tag={tag}\n")
          PY

      - name: Compute lowercase image repo
        id: image
        run: |
          echo "repo=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        if: steps.meta.outputs.tag != ''
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        if: steps.meta.outputs.tag != ''
        uses: docker/setup-buildx-action@v3

      - name: Build and push core image
        if: steps.meta.outputs.tag != ''
        uses: docker/build-push-action@v6
        with:
          context: core
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.image.outputs.repo }}/core:${{ steps.meta.outputs.tag }}
            ghcr.io/${{ steps.image.outputs.repo }}/core:latest
